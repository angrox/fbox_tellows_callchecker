## Fritzbox Call Checker

Small Shell script that tries to fetch the call log of a Fritzbox and checks the number against 
the tellows service.

### Usage 
Edit the file and change the 'fbox' and 'PASSWD' variable and execute the script.

